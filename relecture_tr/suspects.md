# Champs turcs perdus — le controle mecanique

Produit par `python tests/relecture_tr.py --suspects`. **Aucune correction appliquee.**

Ce ne sont pas des traductions faibles, ce sont des donnees perdues : un caractere unique en guise de mot, une phrase de moins de huit caracteres. Le champ existe et n'est pas vide, donc `reste_turc.py` les comptait comme faits et l'application les affiche tels quels.

Ils sont exclus des lots de relecture : envoyer un lot de « c » a un relecteur gaspille le relecteur.

## expressions (200 champs)

### C1 — 200

- **nicht von ungefähr** — `traduction_tr` vaut 'b' (attendu : ce n'est pas un hasard)
- **nicht von ungefähr** — `exemple_tr` vaut 'o' (attendu : ce n'est pas un hasard)
- **im Zuge dessen** — `traduction_tr` vaut 'b' (attendu : dans la foulée)
- **im Zuge dessen** — `exemple_tr` vaut 'u' (attendu : dans la foulée)
- **im Gegenzug** — `traduction_tr` vaut 'b' (attendu : en contrepartie)
- **im Gegenzug** — `exemple_tr` vaut 'u' (attendu : en contrepartie)
- **bei aller Kritik** — `traduction_tr` vaut 'b' (attendu : malgré toutes les critiques)
- **bei aller Kritik** — `exemple_tr` vaut 'ü' (attendu : malgré toutes les critiques)
- **das mag sein, aber** — `traduction_tr` vaut 'o' (attendu : c'est possible, mais)
- **das mag sein, aber** — `exemple_tr` vaut 'l' (attendu : c'est possible, mais)
- **geschweige denn** — `traduction_tr` vaut 'n' (attendu : et encore moins)
- **geschweige denn** — `exemple_tr` vaut 'e' (attendu : et encore moins)
- **im Grunde genommen** — `traduction_tr` vaut 'a' (attendu : au fond)
- **im Grunde genommen** — `exemple_tr` vaut 's' (attendu : au fond)
- **bei näherem Hinsehen** — `traduction_tr` vaut 'd' (attendu : en y regardant de plus près)
- **bei näherem Hinsehen** — `exemple_tr` vaut 'a' (attendu : en y regardant de plus près)
- **angesichts dessen** — `traduction_tr` vaut 'b' (attendu : au vu de cela)
- **angesichts dessen** — `exemple_tr` vaut 'u' (attendu : au vu de cela)
- **vor dem Hintergrund** — `traduction_tr` vaut 'ı' (attendu : dans le contexte de)
- **vor dem Hintergrund** — `exemple_tr` vaut 'ş' (attendu : dans le contexte de)
- **in Ermangelung** — `traduction_tr` vaut 'y' (attendu : à défaut de)
- **in Ermangelung** — `exemple_tr` vaut 'o' (attendu : à défaut de)
- **nach Lage der Dinge** — `traduction_tr` vaut 'i' (attendu : en l'état des choses)
- **nach Lage der Dinge** — `exemple_tr` vaut 'ş' (attendu : en l'état des choses)
- **bei Licht betrachtet** — `traduction_tr` vaut 's' (attendu : à bien y regarder)
- **bei Licht betrachtet** — `exemple_tr` vaut 'e' (attendu : à bien y regarder)
- **in letzter Konsequenz** — `traduction_tr` vaut 'n' (attendu : en dernière analyse)
- **in letzter Konsequenz** — `exemple_tr` vaut 'i' (attendu : en dernière analyse)
- **der Vollständigkeit halber** — `traduction_tr` vaut 'e' (attendu : par souci d'exhaustivité)
- **der Vollständigkeit halber** — `exemple_tr` vaut 'k' (attendu : par souci d'exhaustivité)
- **ungeachtet dessen** — `traduction_tr` vaut 'b' (attendu : nonobstant cela)
- **ungeachtet dessen** — `exemple_tr` vaut 'u' (attendu : nonobstant cela)
- **im Klartext** — `traduction_tr` vaut 'a' (attendu : en clair)
- **im Klartext** — `exemple_tr` vaut 'ç' (attendu : en clair)
- **im Raum stehen** — `traduction_tr` vaut 'k' (attendu : être en suspens)
- **im Raum stehen** — `exemple_tr` vaut 'a' (attendu : être en suspens)
- **sich zu Wort melden** — `traduction_tr` vaut 's' (attendu : demander la parole)
- **sich zu Wort melden** — `exemple_tr` vaut 'ö' (attendu : demander la parole)
- **ich bin der Auffassung** — `traduction_tr` vaut 'ş' (attendu : je suis d'avis)
- **ich bin der Auffassung** — `exemple_tr` vaut 'u' (attendu : je suis d'avis)
- **dem kann ich nur beipflichten** — `traduction_tr` vaut 'b' (attendu : je ne peux qu'approuver)
- **dem kann ich nur beipflichten** — `exemple_tr` vaut 'u' (attendu : je ne peux qu'approuver)
- **meines Erachtens** — `traduction_tr` vaut 'b' (attendu : à mon sens)
- **meines Erachtens** — `exemple_tr` vaut 'e' (attendu : à mon sens)
- **ich neige zu der Ansicht** — `traduction_tr` vaut 'ş' (attendu : je penche pour l'idée)
- **ich neige zu der Ansicht** — `exemple_tr` vaut 'u' (attendu : je penche pour l'idée)
- **ich stehe dem skeptisch gegenüber** — `traduction_tr` vaut 'b' (attendu : je suis sceptique là-dessus)
- **ich stehe dem skeptisch gegenüber** — `exemple_tr` vaut 'u' (attendu : je suis sceptique là-dessus)
- **das halte ich für weit hergeholt** — `traduction_tr` vaut 'b' (attendu : je trouve cela tiré par les cheveux)
- **das halte ich für weit hergeholt** — `exemple_tr` vaut 'u' (attendu : je trouve cela tiré par les cheveux)
- **da bin ich ganz bei dir** — `traduction_tr` vaut 'b' (attendu : je te suis entièrement)
- **da bin ich ganz bei dir** — `exemple_tr` vaut 'u' (attendu : je te suis entièrement)
- **das lässt sich hören** — `traduction_tr` vaut 'k' (attendu : voilà qui est intéressant)
- **das lässt sich hören** — `exemple_tr` vaut 'u' (attendu : voilà qui est intéressant)
- **das geht mir gegen den Strich** — `traduction_tr` vaut 'b' (attendu : ça me hérisse)
- **das geht mir gegen den Strich** — `exemple_tr` vaut 'u' (attendu : ça me hérisse)
- **mir platzt der Kragen** — `traduction_tr` vaut 't' (attendu : je sors de mes gonds)
- **mir platzt der Kragen** — `exemple_tr` vaut 'e' (attendu : je sors de mes gonds)
- **das schlägt dem Fass den Boden aus** — `traduction_tr` vaut 'b' (attendu : c'est la goutte d'eau)
- **das schlägt dem Fass den Boden aus** — `exemple_tr` vaut 'a' (attendu : c'est la goutte d'eau)
- **mir fällt ein Stein vom Herzen** — `traduction_tr` vaut 'i' (attendu : cela m'ôte un poids)
- **mir fällt ein Stein vom Herzen** — `exemple_tr` vaut 'ç' (attendu : cela m'ôte un poids)
- **das ist mir ein Rätsel** — `traduction_tr` vaut 'b' (attendu : c'est un mystère pour moi)
- **das ist mir ein Rätsel** — `exemple_tr` vaut 'u' (attendu : c'est un mystère pour moi)
- **ich bin hin und hergerissen** — `traduction_tr` vaut 'a' (attendu : je suis partagé)
- **ich bin hin und hergerissen** — `exemple_tr` vaut 'r' (attendu : je suis partagé)
- **das lässt mich kalt** — `traduction_tr` vaut 'b' (attendu : cela me laisse froid)
- **das lässt mich kalt** — `exemple_tr` vaut 'u' (attendu : cela me laisse froid)
- **es sträubt sich alles in mir** — `traduction_tr` vaut 'i' (attendu : tout en moi se rebiffe)
- **es sträubt sich alles in mir** — `exemple_tr` vaut 'ç' (attendu : tout en moi se rebiffe)
- **ich könnte aus der Haut fahren** — `traduction_tr` vaut 'ç' (attendu : je pourrais exploser)
- **ich könnte aus der Haut fahren** — `exemple_tr` vaut 'ı' (attendu : je pourrais exploser)
- **das raubt mir den letzten Nerv** — `traduction_tr` vaut 's' (attendu : cela m'achève les nerfs)
- **das raubt mir den letzten Nerv** — `exemple_tr` vaut 'i' (attendu : cela m'achève les nerfs)
- **mir ist es ein Anliegen** — `traduction_tr` vaut 'b' (attendu : cela me tient à cœur)
- **mir ist es ein Anliegen** — `exemple_tr` vaut 'u' (attendu : cela me tient à cœur)
- **da bin ich überfragt** — `traduction_tr` vaut 'o' (attendu : là, je sèche)
- **da bin ich überfragt** — `exemple_tr` vaut 'r' (attendu : là, je sèche)
- **es hält sich in Grenzen** — `traduction_tr` vaut 'ö' (attendu : cela reste modéré)
- **es hält sich in Grenzen** — `exemple_tr` vaut 'l' (attendu : cela reste modéré)
- **das kommt mir spanisch vor** — `traduction_tr` vaut 'b' (attendu : cela me semble louche)
- **das kommt mir spanisch vor** — `exemple_tr` vaut 'u' (attendu : cela me semble louche)
- **vor Wut kochen** — `traduction_tr` vaut 'ö' (attendu : bouillir de rage)
- **vor Wut kochen** — `exemple_tr` vaut 'f' (attendu : bouillir de rage)
- **außer sich sein** — `traduction_tr` vaut 'k' (attendu : être hors de soi)
- **außer sich sein** — `exemple_tr` vaut 'e' (attendu : être hors de soi)
- **zur Sprache bringen** — `traduction_tr` vaut 'g' (attendu : aborder (un sujet))
- **zur Sprache bringen** — `exemple_tr` vaut 'ü' (attendu : aborder (un sujet))
- **Anspruch erheben** — `traduction_tr` vaut 'h' (attendu : revendiquer)
- **Anspruch erheben** — `exemple_tr` vaut 'a' (attendu : revendiquer)
- **Abstand nehmen** — `traduction_tr` vaut 'v' (attendu : renoncer à)
- **Abstand nehmen** — `exemple_tr` vaut 'a' (attendu : renoncer à)
- **unter Beweis stellen** — `traduction_tr` vaut 'k' (attendu : faire la preuve de)
- **unter Beweis stellen** — `exemple_tr` vaut 'a' (attendu : faire la preuve de)
- **zur Rede stellen** — `traduction_tr` vaut 'h' (attendu : demander des comptes à)
- **zur Rede stellen** — `exemple_tr` vaut 'e' (attendu : demander des comptes à)
- **außer Acht lassen** — `traduction_tr` vaut 'd' (attendu : négliger)
- **außer Acht lassen** — `exemple_tr` vaut 'i' (attendu : négliger)
- **Folge leisten** — `traduction_tr` vaut 'g' (attendu : donner suite à)
- **Folge leisten** — `exemple_tr` vaut 'e' (attendu : donner suite à)
- **Widerstand leisten** — `traduction_tr` vaut 'd' (attendu : opposer une résistance)
- **Widerstand leisten** — `exemple_tr` vaut 'i' (attendu : opposer une résistance)
- **Klarheit schaffen** — `traduction_tr` vaut 'd' (attendu : faire la lumière)
- **Klarheit schaffen** — `exemple_tr` vaut 'u' (attendu : faire la lumière)
- **Anstoß erregen** — `traduction_tr` vaut 't' (attendu : faire scandale)
- **Anstoß erregen** — `exemple_tr` vaut 'e' (attendu : faire scandale)
- **den Anstoß geben** — `traduction_tr` vaut 'i' (attendu : donner l'impulsion)
- **den Anstoß geben** — `exemple_tr` vaut 'l' (attendu : donner l'impulsion)
- **Schlüsse ziehen** — `traduction_tr` vaut 's' (attendu : tirer des conclusions)
- **Schlüsse ziehen** — `exemple_tr` vaut 'o' (attendu : tirer des conclusions)
- **Stellung beziehen** — `traduction_tr` vaut 't' (attendu : prendre position)
- **Stellung beziehen** — `exemple_tr` vaut 'a' (attendu : prendre position)
- **Kritik üben** — `traduction_tr` vaut 'e' (attendu : formuler des critiques)
- **Kritik üben** — `exemple_tr` vaut 'l' (attendu : formuler des critiques)
- **Druck ausüben** — `traduction_tr` vaut 'b' (attendu : exercer une pression)
- **Druck ausüben** — `exemple_tr` vaut 'a' (attendu : exercer une pression)
- **Einfluss nehmen** — `traduction_tr` vaut 'e' (attendu : exercer une influence)
- **Einfluss nehmen** — `exemple_tr` vaut 't' (attendu : exercer une influence)
- **Zweifel hegen** — `traduction_tr` vaut 'k' (attendu : nourrir des doutes)
- **Zweifel hegen** — `exemple_tr` vaut 'u' (attendu : nourrir des doutes)
- **Bedenken äußern** — `traduction_tr` vaut 'e' (attendu : émettre des réserves)
- **Bedenken äußern** — `exemple_tr` vaut 'n' (attendu : émettre des réserves)
- **Vorbehalte haben** — `traduction_tr` vaut 'ç' (attendu : avoir des réticences)
- **Vorbehalte haben** — `exemple_tr` vaut 'e' (attendu : avoir des réticences)
- **Zugeständnisse machen** — `traduction_tr` vaut 't' (attendu : faire des concessions)
- **Zugeständnisse machen** — `exemple_tr` vaut 'a' (attendu : faire des concessions)
- **Kompromisse eingehen** — `traduction_tr` vaut 'u' (attendu : accepter des compromis)
- **Kompromisse eingehen** — `exemple_tr` vaut 'z' (attendu : accepter des compromis)
- **ein Risiko eingehen** — `traduction_tr` vaut 'r' (attendu : prendre un risque)
- **ein Risiko eingehen** — `exemple_tr` vaut 'i' (attendu : prendre un risque)
- **Rechenschaft ablegen** — `traduction_tr` vaut 'y' (attendu : rendre des comptes)
- **Rechenschaft ablegen** — `exemple_tr` vaut 'a' (attendu : rendre des comptes)
- **einen Vorschlag unterbreiten** — `traduction_tr` vaut 'ö' (attendu : soumettre une proposition)
- **einen Vorschlag unterbreiten** — `exemple_tr` vaut 'n' (attendu : soumettre une proposition)
- **Aufmerksamkeit schenken** — `traduction_tr` vaut 'd' (attendu : prêter attention)
- **Aufmerksamkeit schenken** — `exemple_tr` vaut 'i' (attendu : prêter attention)
- **Glauben schenken** — `traduction_tr` vaut 'i' (attendu : prêter foi à)
- **Glauben schenken** — `exemple_tr` vaut 'n' (attendu : prêter foi à)
- **Gehör finden** — `traduction_tr` vaut 'k' (attendu : trouver une oreille attentive)
- **Gehör finden** — `exemple_tr` vaut 'u' (attendu : trouver une oreille attentive)
- **Zustimmung finden** — `traduction_tr` vaut 'o' (attendu : recueillir l'adhésion)
- **Zustimmung finden** — `exemple_tr` vaut 'n' (attendu : recueillir l'adhésion)
- **Anwendung finden** — `traduction_tr` vaut 'u' (attendu : s'appliquer, être applicable)
- **Anwendung finden** — `exemple_tr` vaut 'y' (attendu : s'appliquer, être applicable)
- **zum Vorschein kommen** — `traduction_tr` vaut 'g' (attendu : apparaître au grand jour)
- **zum Vorschein kommen** — `exemple_tr` vaut 'ü' (attendu : apparaître au grand jour)
- **ins Stocken geraten** — `traduction_tr` vaut 't' (attendu : s'enliser)
- **ins Stocken geraten** — `exemple_tr` vaut 'ı' (attendu : s'enliser)
- **in Vergessenheit geraten** — `traduction_tr` vaut 'u' (attendu : tomber dans l'oubli)
- **in Vergessenheit geraten** — `exemple_tr` vaut 'n' (attendu : tomber dans l'oubli)
- **in die Wege leiten** — `traduction_tr` vaut 'y' (attendu : mettre en route)
- **in die Wege leiten** — `exemple_tr` vaut 'o' (attendu : mettre en route)
- **auf den Weg bringen** — `traduction_tr` vaut 'h' (attendu : lancer)
- **auf den Weg bringen** — `exemple_tr` vaut 'a' (attendu : lancer)
- **zu Protokoll geben** — `traduction_tr` vaut 't' (attendu : déclarer au procès-verbal)
- **zu Protokoll geben** — `exemple_tr` vaut 'u' (attendu : déclarer au procès-verbal)
- **Einspruch erheben** — `traduction_tr` vaut 'r' (attendu : faire opposition)
- **Einspruch erheben** — `exemple_tr` vaut 'e' (attendu : faire opposition)
- **in Verzug geraten** — `traduction_tr` vaut 'g' (attendu : prendre du retard)
- **in Verzug geraten** — `exemple_tr` vaut 'e' (attendu : prendre du retard)
- **sich in Verbindung setzen** — `traduction_tr` vaut 'i' (attendu : prendre contact)
- **sich in Verbindung setzen** — `exemple_tr` vaut 'l' (attendu : prendre contact)
- **zur Auflage machen** — `traduction_tr` vaut 'ş' (attendu : imposer comme condition)
- **zur Auflage machen** — `exemple_tr` vaut 'a' (attendu : imposer comme condition)
- **in Rechnung stellen** — `traduction_tr` vaut 'f' (attendu : facturer)
- **in Rechnung stellen** — `exemple_tr` vaut 'a' (attendu : facturer)
- **Vorsorge treffen** — `traduction_tr` vaut 'ö' (attendu : prendre des précautions)
- **Vorsorge treffen** — `exemple_tr` vaut 'n' (attendu : prendre des précautions)
- **Bericht erstatten** — `traduction_tr` vaut 'r' (attendu : faire rapport)
- **Bericht erstatten** — `exemple_tr` vaut 'a' (attendu : faire rapport)
- **Anzeige erstatten** — `traduction_tr` vaut 'ş' (attendu : porter plainte)
- **Anzeige erstatten** — `exemple_tr` vaut 'i' (attendu : porter plainte)
- **Widerspruch einlegen** — `traduction_tr` vaut 'i' (attendu : former un recours)
- **Widerspruch einlegen** — `exemple_tr` vaut 't' (attendu : former un recours)
- **Beschwerde einreichen** — `traduction_tr` vaut 'ş' (attendu : déposer une réclamation)
- **Beschwerde einreichen** — `exemple_tr` vaut 'i' (attendu : déposer une réclamation)
- **in die Tat umsetzen** — `traduction_tr` vaut 'u' (attendu : mettre en pratique)
- **in die Tat umsetzen** — `exemple_tr` vaut 'y' (attendu : mettre en pratique)
- **eine Entscheidung herbeiführen** — `traduction_tr` vaut 'k' (attendu : provoquer une décision)
- **eine Entscheidung herbeiführen** — `exemple_tr` vaut 'a' (attendu : provoquer une décision)
- **Vorkehrungen treffen** — `traduction_tr` vaut 'g' (attendu : prendre des dispositions)
- **Vorkehrungen treffen** — `exemple_tr` vaut 'e' (attendu : prendre des dispositions)
- **sehr geehrte Damen und Herren** — `traduction_tr` vaut 'S' (attendu : Madame, Monsieur)
- **sehr geehrte Damen und Herren** — `exemple_tr` vaut 'a' (attendu : Madame, Monsieur)
- **mit freundlichen Grüßen** — `traduction_tr` vaut 'S' (attendu : cordialement)
- **mit freundlichen Grüßen** — `exemple_tr` vaut 'a' (attendu : cordialement)
- **hochachtungsvoll** — `traduction_tr` vaut 'D' (attendu : veuillez agréer mes salutations distinguées)
- **hochachtungsvoll** — `exemple_tr` vaut 'e' (attendu : veuillez agréer mes salutations distinguées)
- **vielen Dank im Voraus** — `traduction_tr` vaut 'Ş' (attendu : merci d'avance)
- **vielen Dank im Voraus** — `exemple_tr` vaut 'i' (attendu : merci d'avance)
- **ich melde mich bezüglich** — `traduction_tr` vaut 'ş' (attendu : je vous contacte au sujet de)
- **ich melde mich bezüglich** — `exemple_tr` vaut 'u' (attendu : je vous contacte au sujet de)
- **ich wäre Ihnen dankbar, wenn** — `traduction_tr` vaut 'ş' (attendu : je vous serais reconnaissant de)
- **ich wäre Ihnen dankbar, wenn** — `exemple_tr` vaut 'u' (attendu : je vous serais reconnaissant de)
- **ich erlaube mir, Sie darauf hinzuweisen** — `traduction_tr` vaut 'd' (attendu : je me permets de vous signaler)
- **ich erlaube mir, Sie darauf hinzuweisen** — `exemple_tr` vaut 'i' (attendu : je me permets de vous signaler)
- **für Rückfragen stehe ich zur Verfügung** — `traduction_tr` vaut 'S' (attendu : je reste à votre disposition)
- **für Rückfragen stehe ich zur Verfügung** — `exemple_tr` vaut 'o' (attendu : je reste à votre disposition)
- **in der Anlage finden Sie** — `traduction_tr` vaut 'E' (attendu : vous trouverez ci-joint)
- **in der Anlage finden Sie** — `exemple_tr` vaut 'k' (attendu : vous trouverez ci-joint)

